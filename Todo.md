mplement the functionality to add parking slots to a specific lot.
add a "Generate Slots" feature that automatically creates slots (e.g., A01-A20) for a new parking lot.
add a simple profile page for the user to edit their phone number and vehicle registration
add a feature to allow drivers to cancel their bookings if they are still in the 'active' state but haven't started yet.
make the admin dashboard dynamic, so that it pulls data feom the database.
make the analytics page on the admin dynamic and the export functions(pdf and excel) for each of the cards dipalying the analytics:
    Revenue Analytics(Platform earnings and trends)
    Usage Analytics (Booking patterns and trends)
    User Analytics(User growth and activity)
 I need to do this without changing the UI, only make the API calls work, first I shall make the API  calls to fetch the data for the admin dashboard then  move on to the analytics functionality.


 I SHALL BE BACK......
